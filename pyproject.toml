[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tensorwright"
version = "0.1.0"
description = "Hardware-aware compiler and cross-layer verification platform for quantized AI accelerators"
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [{ name = "Hank Lee" }]
dependencies = [
    "numpy>=1.23.2,<3",
    "onnx>=1.16,<2",
]

[project.optional-dependencies]
dev = [
    "cocotb>=2.0,<3; python_version < '3.14'",
    "mypy>=1.10",
    "ruff>=0.5",
]

[project.scripts]
tensorwright = "tensorwright.cli:main"

[tool.setuptools.packages.find]
include = ["tensorwright*"]

[tool.ruff]
line-length = 88
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.12"
strict = true
explicit_package_bases = true
files = ["tensorwright"]
disable_error_code = ["no-any-return", "var-annotated"]

[[tool.mypy.overrides]]
module = ["numpy", "numpy.*", "onnx", "onnx.*"]
follow_imports = "skip"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["cocotb", "cocotb.*", "cocotb_tools", "cocotb_tools.*"]
follow_imports = "skip"
ignore_missing_imports = true
