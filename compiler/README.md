# Compiler staging area

The Milestone 1 bit-accurate integer reference lives in `tensorwright.reference` so it
can be shared by compiler and verification code. Compiler implementation begins in
Milestone 2 with ONNX frontend and typed IR modules. This directory records the intended
subsystem boundary without presenting placeholder implementations as complete.
